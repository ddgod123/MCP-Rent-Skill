# Changelog

## 0.10.1

- 重写 frontmatter `description`：改为「使用场景 + 用户常见说法（触发词）+ 负面触发」的写法，
  提升宿主触发准确率；移除开发向表述「面向 WorkBuddy、Codex、Claude 等 MCP 宿主」。
- `metadata.version` 提升至 `0.10.1`。
- 物业类型选项提示由「回复数字：1/2/3」改为「回复字母：A/B/C」（源头是治理目录的
  `intake_hint` 配置）；同时把 §2 的歧义举例
  从「填写物业类型时的 1/2/3」改为例举「追问居室时的 3 / 房源列表后的 2」——
  目的是把数字 1–9 让给快捷菜单，消除填表场景与菜单的数字歧义。
- 房源列表与楼盘表的序号改用**带圈数字**（`①②③`），并在 §2 增加「序号归属」硬规则：
  阿拉伯数字 `1`–`9` 只属于快捷菜单，宿主自产序号一律用带圈数字。
- 数字判定改为两步：**形态判定**（去掉首尾空白与结尾标点、全角转半角后，整条形如 `^[1-9]$`）
  + **上下文判定**（当前有待作答问题时，数字按该问题解释，主要是「追问居室」）；并写明
  未定义编号（`4`/`5`/`7`/`8`/`9`）的固定话术与"不调用任何工具"。
- 将低频工具路由和客户回复契约拆到 `references/tool-routing.md`、
  `references/response-format.md`，`SKILL.md` 改为按需读取指针；主流程规则仍留在入口文件。
- 发布 manifest 升级为 schema v2，支持 `SKILL.md`、references 和更新器逐文件下载、校验、
  原子写入与失败回滚；更新器继续兼容 schema v1。
- WorkBuddy 项目安装会同步安装 references、manifest 和更新器，并为已有文件创建备份。

> 发布流程：执行 `scripts/publish_rentpro_skill.py` 重算每个文件的 sha256 并更新 manifest，
> 检查公开仓库 diff 后提交并推送；宿主侧再执行
> `python3 scripts/rentpro_skill_update.py install` 或 `update --yes`。

## 0.10.0

- 需求模板改为**服务端目录驱动**：字段、顺序、必填、首轮展示范围由
  `basic_catalog` / `show_intake` / `search_role` 决定，Skill 不再自带字段定义。
- 新增**菜单分流**：菜单 1 开启/续接租赁咨询，2 查楼盘，3 帮助手册，6 联系房源经理（占位）；
  补齐数字消歧规则与「菜单」全局回指。
- 深度筛选改为**整组替换合并**：先读目录与现有条件，按 `filter_code` 合并，未提及的条件保留。
- 新增 `property_url` 客户楼盘资料页指引。
- 补充缺失提示、展示边界与无结果处理条款。

## 0.9.0

- 新增楼盘级挂牌浏览工作区、匿名会话收藏/排除状态和收藏房源查询工具。

## 0.6.0

- 首次公开发布 `rentpro-rent` Skill。
- 增加 WorkBuddy 本地安装和刷新说明。
- 增加 GitHub manifest、版本检查和用户确认后的更新流程。
